export interface AnimationInventory {
  detector: string;
  triggers: string[];
  selector: string;
  properties: string[];
  triggerDetails: string[];
  confidence: number;
}

export interface AnimationInfo {
  triggers: string[];
  selector: string;
  properties: string[];
  triggerDetails: string[];
  confidence: number;
}
